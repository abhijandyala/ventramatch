# AGENTS.md — VentraMatch

Instructions for any AI coding agent (Cursor, Claude Code, Codex, Windsurf, etc.) working on this repo. Read this **before** writing any code or making any plan.

## Read order on first connection

1. `README.md` — what VentraMatch is, stack, layout, scripts.
2. `PRODUCT.md` — product truth, users, voice, anti-references.
3. `DESIGN.md` — design tokens, components, anti-patterns.
4. `docs/architecture.md` — system architecture and data model.
5. `docs/legal.md` — patents to avoid, compliance constraints.
6. This file (`AGENTS.md`) — execution rules.

Optional: `docs/initial-brief.md` — original kickoff, Google Doc / PDF note, and commands to reinstall `impeccable` and `ui-ux-pro-max` if `.cursor/skills` is incomplete.

If any of the six files above is missing on a fresh machine, **stop and tell the human**. Do not improvise the contents.

## Hard rules

1. **Use the installed design skills for all UI work.** Both `impeccable` and `ui-ux-pro-max` live in `.cursor/skills/`. Default workflow: `/impeccable craft <feature>` for new UI, `/impeccable critique` and `/impeccable polish` before merging, `ui-ux-pro-max` for fresh page-level systems.
2. **Never invent design tokens.** All colors, fonts, spacing, radii come from `DESIGN.md` → `app/globals.css`. If you need a new token, add it to `DESIGN.md` first.
3. **No AI-slop UI.** If the result could be tagged "AI made that" at a glance, throw it out. Re-run with the right register and a sharper scene sentence.
4. **Row-Level Security on every Postgres table.** Same migration as the table. Policies use `app_user_id()`; the app sets `ventramatch.user_id` per transaction via `lib/db.ts` `withUserRls`. No exceptions.
5. **Server Components by default.** Mark a component `"use client"` only when it needs state, browser APIs, or event handlers. Audit annually.
6. **Validate inputs with Zod** at every server action and route handler. Never trust client payloads.
7. **No success fees, no investment advice, no funding promises.** See `PRODUCT.md` and `docs/legal.md`. Disclaimer on every match score.
8. **Avoid Tinder's patented match flow as described in `docs/legal.md`.** Especially: do not ship a full-bleed photo card with bottom-row two-circle action buttons.
9. **Never commit secrets.** `.env.local` is git-ignored. Anything sensitive lives there or in the host’s env (e.g. Railway).
10. **Never push to `main` directly.** Branch + PR. Even for solo work.

## Workflow when making changes

1. State what you're going to do in plain English before any tool calls if the change is non-trivial.
2. Read the file before editing. Always.
3. Make the smallest change that solves the problem.
4. Run `npm run typecheck` and `npm run lint` after substantive edits.
5. Run `kluster_code_review_auto` per the project rule in `.cursor/rules/kluster-code-verify.mdc`.
6. Open a PR with a 2-line summary and a test plan.

## Workflow for new dependencies

1. Add to `package.json`.
2. Run `kluster_dependency_check` (mandatory per `.cursor/rules/kluster-code-verify.mdc`).
3. Only after the check passes, run `npm install`.

## File conventions

- TypeScript strict mode, no `any` without a comment explaining why.
- Components in `PascalCase.tsx`, hooks in `useCamelCase.ts`, utils in `kebab-case.ts`.
- Tailwind classes in declaration order: layout → spacing → sizing → typography → color → state. Use `cn()` from `lib/utils.ts` to merge classes.
- Server actions in `app/<route>/actions.ts`. Each action takes a Zod-parsed input.
- SQL migrations are append-only. Once committed, never edit. Add a new migration to fix a previous one.

## Comments

- No narration comments (`// Set the value`).
- No "AI" giveaway phrases (`// Generated by ...`, `// Optimized for ...`, `// Best practices applied`).
- Comments only for non-obvious intent: trade-offs, constraints, references to specs.

## Communication

- Use the user's repo / file names exactly. Don't paraphrase paths.
- When citing files in chat, use repo-relative paths (`app/feed/page.tsx`), not absolute.
- When proposing schema changes, show the SQL diff first; don't apply silently.

## When in doubt

Stop. Ask. The cost of asking is one message. The cost of building the wrong thing is a day.
